<form method="post" action="{{ path('app_tab_coach_delete', {'idCoach': tab_coach.idCoach}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tab_coach.idCoach) }}">
    <button class="btn">Delete</button>
</form>
