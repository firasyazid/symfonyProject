<form method="post" action="{{ path('app_facture_delete', {'idF': facture.idF}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.idF) }}">
    <button class="btn">Delete</button>
</form>
