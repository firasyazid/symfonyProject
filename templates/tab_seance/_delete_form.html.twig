<form method="post" action="{{ path('app_tab_seance_delete', {'idSeance': tab_seance.idSeance}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tab_seance.idSeance) }}">
    <button class="btn">Delete</button>
</form>
